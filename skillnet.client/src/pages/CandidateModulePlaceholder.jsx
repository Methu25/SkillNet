const CandidateModulePlaceholder = ({ title }) => (
    <main style={{ maxWidth: '900px', margin: '48px auto', padding: '24px' }}>
        <h1>{title}</h1>
        <p>This Candidate Module page is ready for its future interface.</p>
    </main>
);

export default CandidateModulePlaceholder;

